spring:
  application:
    name: auction

  config:
    import: optional:application-secret.yml

  datasource:
    url: jdbc:mysql://localhost:3306/auction?serverTimezone=UTC&characterEncoding=UTF-8
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: ${mysql.username}
    password: ${mysql.password}

  jpa:
    hibernate:
      ddl-auto: update   # create, create-drop, update, validate 중 선택
    properties:
      hibernate:
        format_sql: true
        show_sql: true

  jwt:
    secret-key: ${jwt.secret-key}
    access-token-validity-seconds: ${jwt.access-token-validity-seconds}
    refresh-token-validity-seconds: ${jwt.refresh-token-validity-seconds}